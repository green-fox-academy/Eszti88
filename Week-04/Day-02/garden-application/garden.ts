class Garden {

    protected flower: Flower = [];
    protected tree: Tree = [];
    
    constructor() {
        this.flower = flower;
        this.tree = tree;
    }

    addFlower(flower: Flower) {
        this.flower.push(flower);
    }

    addTree(tree: Tree) {
        this.tree.push(tree);
    }
}

