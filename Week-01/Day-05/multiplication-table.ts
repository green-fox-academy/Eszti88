// number=15, create a program that prints the
// multiplication table with number

let number: number = 15;

for (let multiplier: number = 1; multiplier < 11; multiplier++) {
	console.log(`${multiplier} * ${number} = ${multiplier * number}`);
}
